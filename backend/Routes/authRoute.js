import express from "express";
// import User from "../config/Model/userModel";

const route = express.Router();

// route.post("/login", (req, res) => {
//   const { fullName, email, password, cpassword } = req.body;
// });

export default authRoute;
